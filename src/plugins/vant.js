import Vue from 'vue'

import {
  NavBar, Cell, Button,
  Toast, Dialog, Icon, Lazyload
} from 'vant'

Vue
  .use(NavBar)
  .use(Toast)
  .use(Dialog)
  .use(Cell)
  .use(Button)
  .use(Icon)
  .use(Lazyload)
